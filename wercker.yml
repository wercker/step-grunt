# box: mhart/alpine-node:5.10.1
# build:
#     steps:
#         - shellcheck:
#             files: run.sh

#         - script:
#             name: prepare output
#             code: rm -rf $WERCKER_ROOT/.git

# box: wercker/default
# build:
#   steps:
#     - validate-wercker-step
