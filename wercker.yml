box: 
  id: mhart/alpine-node:5.10.1
  cmd: /bin/sh
build:
  steps:
    # - shellcheck:
    #     files: run.sh
    # - script:
    #     name: prepare output
    #     code: rm -rf $WERCKER_ROOT/.git
    - validate-wercker-step
